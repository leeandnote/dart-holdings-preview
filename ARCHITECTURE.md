# 운영 설계: DART 대량보유 지분변동 서비스

## 결론

공개 사이트에서는 사용자가 접속할 때 DART API를 직접 호출하지 않습니다.

권장 구조는 다음과 같습니다.

1. 스케줄러가 하루 1~2회 DART API를 호출합니다.
2. 원본 공시와 정규화 데이터를 내 DB에 저장합니다.
3. 분석 로직이 종목별 수급 해석 콘텐츠를 생성합니다.
4. 웹사이트는 내 DB/API 또는 정적 JSON 캐시만 읽습니다.

## 수집 주기

- 장 마감 후 16:30~18:00: 당일 공시 1차 반영
- 새벽 05:00: 정정 공시, 누락분, 전일 데이터 재검증
- 필요 시 장중 수동 갱신 버튼은 관리자만 사용
- 가격 캐시는 공시 접수일 필터와 분리해서 매 갱신 시점의 최신 일봉 종가까지 갱신

## 저장 대상

- `주식등의대량보유상황보고서(일반)`
- `주식등의대량보유상황보고서(약식)`
- 코스피, 코스닥 상장사만 포함
- 최근 3개월은 빠른 조회용 캐시
- 전체 원본은 장기 분석용으로 누적

## 페이지 구성

- 전체 이슈 랭킹: 5% 상향돌파, 지분 증가폭, 보고 빈도 기준
- 종목 상세: TradingView 일봉 차트 + 공시 타임라인
- 자동 해석: 수급 변화, 5% 돌파 의미, 리스크 문장 자동 생성
- PDF 저장: 일별/주별/월별 리포트 인쇄

## 데이터 소스 판단

TradingView 위젯은 차트 품질과 사용자 경험이 좋고 무료로 임베드하기 쉽습니다. 다만 무료 위젯 위에 임의의 공시 마커를 직접 그리는 자유도는 제한됩니다.

그래서 1차 버전은 `TradingView 차트 + 공시 타임라인 + 자동 해석` 구조가 적합합니다.

공시 마커를 캔들 위에 정확히 찍으려면 2차 버전에서 가격 데이터를 DB에 별도로 저장하고 Lightweight Charts, ApexCharts 같은 차트 라이브러리로 직접 그리는 편이 좋습니다.

## 애드센스용 콘텐츠 자동화

원본 공시 표만 보여주면 가치가 약합니다. 각 종목 페이지에는 최소한 다음 문단을 자동 생성합니다.

- 지분율 변화 요약
- 5% 신규 진입 또는 이탈 여부
- 보고자 성격: 연기금, 자산운용, 최대주주, 개인 등
- 수급 해석: 하방 지지 가능성, 차익 실현 가능성, 추격 위험
- 차트 확인 포인트: 신고일 전후 거래량, 갭, 이동평균선 위치

문장은 규칙 기반으로 먼저 만들고, 추후 관리자 검수 또는 LLM 요약을 붙이는 방식이 안전합니다.
